import LanguageSwitcher from './language-switcher.svelte';
import type { Language, LanguageSwitcherProps } from './types';

export { LanguageSwitcher, type Language, type LanguageSwitcherProps };

// Also export as Root for consistency with other shadcn components
export { LanguageSwitcher as Root };
